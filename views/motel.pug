extends layout 

block content
  h1= title
  - let motel = results;
  script.
    function buy(x){
      alert('Purchased Item: ' + x)
   } 
  div(class='container-fluid')
    div(class='row m-1')
      each m, index in motel 
       - let destination = `dispatch(\"/motel/update?id=${m._id}\")` 
       div(class="col-sm-2" onclick=`buy(${index})`) #{m.motel_type} #{m.Duration} #{m.Cost}
       button.btn( class="btn-primary ml-1", onclick= destination) Update 
  script. 
    function dispatch(path){ 
      window.location.href=path 
    } 